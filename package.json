{
  "name": "threads-text-archive-draft",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test --test-concurrency=1 tests/*.test.mjs",
    "test:extension": "node --test --test-concurrency=1 tests/extension-*.test.mjs",
    "demo": "node src/cli.mjs --account demo_account --output output --source demo"
  },
  "devDependencies": {
    "fake-indexeddb": "^6.2.5",
    "jsdom": "^26.1.0"
  }
}
