# 인수 항목별 대응표 — 2026-09-09

원본 `ACCEPTANCE-CHECKLIST.md`의 24개 항목을 순서대로 대응한다. 원본 체크박스는 실제 서비스의 인수 완료를 의미할 수 있어 변경하지 않았다. 아래 **구현 및 검증 완료**도 명시된 로컬/fixture 범위에 한한다. 실제 Threads 자동 수집은 미구현이다.

상태는 요청한 네 범주를 사용한다: 구현 및 검증 완료 / 구현했으나 실제 서비스 검증 필요 / 미구현 / 현재 기술적으로 확인 불가. `현재 기술적으로 확인 불가`는 불가능하다는 단정이 아니라 이번 조사에서 필요한 서비스 데이터·동작의 근거를 확보하지 못했다는 뜻이다.

## 수집 범위

1. **공개 계정명을 입력하여 수집할 수 있다 — 미구현.** 입력·정규화·로컬 fixture 실행은 검증했다. 실제 계정 게시물 조회는 연결되지 않았다.
2. **일반 원글 본문이 저장된다 — 구현했으나 실제 서비스 검증 필요.** 정규화된 원글 본문 저장 테스트 통과. 웹/API 추출 없음.
3. **작성 시 한꺼번에 만든 게시물 묶음만 하나로 합쳐진다 — 현재 기술적으로 확인 불가.** 합성 evidence 기반 결합·순번 검증 테스트 통과. 실제 동시 작성 관계를 판별하는 데이터는 미확인.
4. **나중에 추가한 자기 답글은 제외된다 — 구현했으나 실제 서비스 검증 필요.** reply 분류 제외 검증. 실제 원천에서 최초 묶음과 구별하는 부분은 미확인.
5. **다른 사용자의 답글과 댓글은 제외된다 — 구현했으나 실제 서비스 검증 필요.** reply/비대상 actor 제외 테스트 통과. 실제 추출 검증 없음.
6. **단순 리포스트는 원글 한 개의 본문만 저장된다 — 구현했으나 실제 서비스 검증 필요.** 한 부분만 허용, 리포스트 표기, 미확인 시점 실패 검증. 원문 및 리포스트 시점 실제 추출은 미확인.
7. **인용 게시물은 대상 계정이 덧붙인 본문만 저장된다 — 구현했으나 실제 서비스 검증 필요.** 정규화된 own-text만 저장. 실서비스 분리 없음.
8. **미디어만 있는 게시물은 제외된다 — 구현했으나 실제 서비스 검증 필요.** 빈 텍스트는 Markdown 흔적 없이 제외. 실제 첨부/본문 유무 판정 없음.
9. **긴 텍스트 첨부물 전문이 저장된다 — 현재 기술적으로 확인 불가.** 긴 정규화 텍스트 5,000자 이상 보존 테스트는 통과. 실제 펼치기·전문 확보 없음.

## 출력

10. **계정당 Markdown 파일 하나가 생성된다 — 구현 및 검증 완료.** CLI·GUI 데모에서 파일 생성 확인.
11. **오래된 글부터 정렬된다 — 구현 및 검증 완료.** 시간대 포함 시각으로 정렬하고 같은 시각은 ID로 안정 정렬.
12. **날짜는 한국 기준 YYYY-MM-DD로 기록된다 — 구현 및 검증 완료.** Asia/Seoul 날짜 경계 테스트. 날짜를 넘는 묶음 대표 날짜는 임시 정책으로 불완전 처리.
13. **본문 속 링크, 줄바꿈, 이모지가 보존된다 — 구현 및 검증 완료.** 원문은 상태에 보존, Markdown은 문자 이스케이프와 명시적 줄바꿈. 링크의 원시 바이트 동일성/자동 링크화는 약속하지 않는다.
14. **별개 게시물 사이에 --- 한 줄이 들어간다 — 구현 및 검증 완료.** 앞뒤 빈 줄과 같은 날짜 반복 제목 검증.
15. **확인되지 않은 순번을 프로그램이 만들지 않는다 — 구현 및 검증 완료.** evidence 없는 label에 실패, 무순번 묶음에는 이어지는 글만 출력.
16. **미디어와 게시물 원본 URL은 Markdown에 들어가지 않는다 — 구현 및 검증 완료.** 메타데이터 필드는 입력 계약에서 거부하고 Markdown에 넣지 않음. 실제 본문과 메타데이터를 분리하는 어댑터는 미구현.

## 안정성과 검증

17. **로그인 없이 먼저 시도한다 — 미구현.** 새 비로그인 Chrome 창을 여는 수동 조사 기능만 구현. 실제 자동 수집 시도는 없음.
18. **필요할 때만 전용 Chrome 창에서 사용자가 직접 로그인한다 — 미구현.** 필요성 감지·멈춤·계속하기 연결 없음. 수동 전용 Chrome 버튼은 코드 구현 상태이며 Chrome 실행/로그인은 실검증하지 않음.
19. **한 번에 한 계정만 순차적으로 수집한다 — 구현 및 검증 완료.** GUI 중복 시작 방지, 수집 파일 잠금, 순차 fetch 테스트. 다른 Windows 사용자 세션 간 잠금은 범위 밖.
20. **보안 확인이나 접근 제한이 나타나면 중단한다 — 구현했으나 실제 서비스 검증 필요.** 합성 오류 코드 발생 시 한 번 호출 후 즉시 정지 및 불완전. 실제 화면/HTTP 응답 감지 없음.
21. **중단 후 이어받기가 가능하다 — 구현 및 검증 완료.** 사용자 중지, 강제 프로세스 종료, GUI 이어받기 테스트. 영구 저장된 원본 상태 자체의 손상 복구는 미구현이며 오류로 중지.
22. **중복 게시물이 한 번만 저장된다 — 구현 및 검증 완료.** 동일 ID 중복 제외 및 내용 충돌 불완전 테스트. 실제 ID의 안정성은 어댑터 검증 과제.
23. **수집 상태 파일과 결과 보고서가 생성된다 — 구현 및 검증 완료.** 필수 필드별 누락 거부와 파일 생성, 저장 오류 시 완료 금지 검증.
24. **누락이 의심되면 결과가 불완전으로 표시된다 — 구현 및 검증 완료.** 소스 범위 미확인, 부분 실패, 위치 반복/계약 오류, 실제 수집 골격의 미지원 사유 처리. 누락 신호를 실제 서비스에서 감지하는 것은 미구현.

## 추가 요구 검증

- 일반/묶음/인용/리포스트, 제외·중복·KST·정렬·정확한 실패 경고·필수 스키마·완료/불완전/중단을 자동 테스트한다.
- Windows WinForms 자동 smoke에서 계정/출력값 입력, 시작, 중지, 이어받기, 발견/저장 수, 완료/부분 실패/실제 골격 불완전을 검증했다.
- 폴더 선택 대화상자에서 사람의 클릭, 탐색기 더블클릭, Chrome 시작, 직접 로그인, 실계정 목록 탐색은 수동 검증하지 않았다.
